/**
 * Server re-export of shared storyboard pricing (keeps Next imports under lib/).
 */
export {
  STORYBOARD_USD_ESTIMATES,
  defaultPricingConfig,
  usdToCredits,
  estimateStoryboardCredits,
} from '../packages/studio/src/storyboard-pricing.js';
